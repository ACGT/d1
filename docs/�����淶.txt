1：meta keywords ,description,title,图片alt要写好，H1、sitemap等基本SEO，每个页面的title都加上“D1优尚网-”。
2：页面URL伪静态化，如http://www.d1.com.cn/product/1200025，原来的asp商品链接跳转到新的链接。
3：Post的地方防止重复提交，按钮屏蔽，显示“正在提交...”，最好做个动画效果。
4：操作代码做权限判断，比如取消订单要判断session的用户和订单的userId是不是相等，免得登陆用户通过传参数操作别人的订单。
5：浏览器兼容：IE6-9，360浏览器，FF，Google，搜狗浏览器，TT。
6：列表页、首页、搜索页、商品详情页都应该加上lazy load。
7：注释和文档（代码要写注释，文档可以以后再写）。
8：多用ajax交互，少用页面跳转交互，如修改密码成功、加购物车、加收藏。
9：页面链接全部用相对路径，方便测试，图片可用绝对路径。
10：统一ajax和jquery框架，对话框、Ajax都用同一个框架。
12：Jsp全部用UTF-8编码。
13：统一提示框，做一个提示框，弹出提示后，3秒淡化并自动关闭，如“修改密码成功”的提示。
14：SEO优化，每个页面都产生相关性高的页面+内容越多越好。
15：session全部用memcached处理，一方面实现分布式session，一方面重启不会丢失session。
16：Jsp目录分机别乱了，ajax、inc、active等，活动页面别加入CVS。
17：考虑到用memcached做缓存，每次调用BaseManager的get方法时都会取到一个新的对象，修改对象的时候注意，只取一次，然后修改。
18：改版后首页二级页标题允许加粗加红等特效，注意图片alt的内容不能有<font>等标记。
    
